import type { Metadata } from "next";
import Link from "next/link";
import { Check } from "lucide-react";
import { packages } from "../../content/site";
import { PageHero } from "../../components/PageHero";
export const metadata:Metadata={title:"Packages",description:"Editable decoration package tiers for celebrations in Indore. Prices available on request."};
const rows=["Backdrop","Balloon arrangement","Personalised signage","Lighting","Theme customisation","Cake-table styling","Floral accents","Welcome detail","Props","Photography corner","Setup team","Add-on options"];
export default function Packages(){return <><PageHero eyebrow="Flexible celebration packages" title="A starting point for your vision." copy="Choose the scale that feels closest to your event. Every confirmed quotation is tailored to the venue, date and creative requirements."/><section className="page-content"><div className="package-row">{packages.map(p=><article className={`package ${p.tone}`} key={p.name}><span>Celebrate Life</span><h3>{p.name}</h3><p>{p.ideal}</p><strong>Price available on request</strong><ul>{p.features.map(f=><li key={f}><Check/>{f}</li>)}</ul><Link href={`/book?package=${p.name}`}>Enquire for {p.name}</Link></article>)}</div><h2 style={{fontSize:"clamp(3rem,6vw,6rem)",margin:"100px 0 35px"}}>Compare the <em>possibilities.</em></h2><table className="comparison"><thead><tr><th>Decoration element</th>{packages.map(p=><th key={p.name}>{p.name}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={r}><td data-label="Element">{r}</td>{packages.map((p,j)=><td data-label={p.name} key={p.name}>{i<3+j?"Discuss":"Optional"}</td>)}</tr>)}</tbody></table><p className="placeholder-note">This comparison is a planning guide, not a promise of inclusions. Final package contents and travel availability are confirmed in writing.</p></section></>}
